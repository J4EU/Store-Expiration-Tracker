## Planning

- 연결된 Issue가 있으면 목표와 완료 조건을 먼저 확인한다.
- 연결된 Issue가 없으면 이번 작업의 단일 목적과 완료 조건을 먼저 정리한다.
- 작업 전에 예상 산출물(문서, 템플릿, 설정 파일 등)을 먼저 나열한다.
- 작업 중 범위가 커지면 현재 Issue/PR에 포함할지, 새로운 Issue로 분리할지 먼저 판단한다.
- 관련 PR, README, 문서가 있으면 작업 전에 함께 확인한다.
- 작업 계획을 먼저 정리하고, 목표와 충돌하는 변경은 사용자와 먼저 논의한다.

## Scope

- Issue 범위를 벗어나는 변경은 하지 않는다.
- 개선 사항이 보이면 코드 변경 대신 별도 후속 작업으로 제안한다.

## Issue 작성 규칙

- Issue 본문은 `.github/ISSUE_TEMPLATE/task.md` 구조를 따른다.
- 문제, 목표, 이번 이슈에서 할 것, 이번 이슈에서 바로 하지 않을 것, 완료 조건을 구분해서 작성한다.
- 실제 배포/운영 검증이 범위에 포함되는지 repo 기준 설정 정리인지 명확히 적는다.

## Git

- Commit 메시지는 `type: 작업 내용` 형식으로 작성한다.
- Commit 메시지의 prefix는 영어, 작업 내용은 한국어로 작성한다.
- 브랜치 생성 시 `type/이름` 형식으로 작성한다.
- `type`은 작업 성격에 맞게 고른다. 사용자 기능은 `feat`, 버그 수정은 `fix`, 문서만 수정하면 `docs`, repo 운영/설정 정리는 `chore`를 사용한다.
- 브랜치 생성 시 `codex/` prefix를 사용하지 않는다.
- 하나의 브랜치는 하나의 Issue 또는 하나의 목적만 가진다.

## Pull Request 작성 규칙

- PR 본문은 `.github/pull_request_template.md` 구조를 따른다.
- 변경 파일을 단순 나열하지 말고, 변경된 동작과 책임을 중심으로 작성한다.
- PR 본문은 필요한 설명을 문단으로 쓰되, 변경점·영향·검증처럼 빠르게 훑어야 하는 내용은 짧은 리스트로 정리한다.
- 실제 diff와 검증 결과에서 확인되는 내용만 작성한다.
- 수행하지 않은 테스트나 검증을 수행한 것처럼 작성하지 않는다.
- 이번 PR에서 다루지 않는 항목과 후속 작업을 구분해서 작성한다.
- 관련 Issue가 있을 때만 PR 본문 마지막에 `Related to #번호` 형식으로 연결한다.
- Issue를 종료할 조건이 완벽히 충족된 경우 `Closes #번호` 형식으로 연결한다.
- PR 설명은 구현 과정보다 최종 상태와 변경 이유를 중심으로 작성한다.
